# NEVER commit these — secrets and credentials
.env
.env.*
*.secrets
*.key
*.pem
credentials*
secrets/
.codig.env

# Python
*.pyc
__pycache__/
*.egg-info/
dist/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Node / frontend
node_modules/
build/

# Data / runtime
data/
*.log
.coverage
.coverage.*
htmlcov/

# Reports — keep locally until ready to publish
docs/reports/

# OS
.DS_Store


# (Forks keep their own private overlay + identity denylist in their own
# .gitignore additions; the motor has nothing fork-specific to ignore here.)
